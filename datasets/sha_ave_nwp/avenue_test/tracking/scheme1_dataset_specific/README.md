# scheme1_dataset_specific

Tracking outputs for `avenue_test` under `scheme1_dataset_specific`.

Generated files:

- `frames/`: one JSONL per video, one record per frame.
- `tracks/`: one JSON summary per video.
- `visualizations/`: rendered tracking videos.
- `run_summary.json`: run-level summary.
